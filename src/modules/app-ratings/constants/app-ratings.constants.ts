export const MIN_RATING = 1;
export const MAX_RATING = 5;

// app ratings properties descriptions
export const ratingDescription = `An integer representing the rating given to the app. Must be a positive number between ${MIN_RATING} and ${MAX_RATING}`;
